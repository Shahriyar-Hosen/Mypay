import { FullSlider } from "@/components/common";

export const Supports = () => {
  return (
    <section className="space-y-5">
      <h1 className="text-3xl font-semibold text-[#dcdcdc] text-center">
        Nos supports de paiement
      </h1>
      <FullSlider />
    </section>
  );
};
